export default class Pokemon {
  constructor(name, weight, height, image, pokeType) {
    this.name = name;
    this.weight = weight;
    this.height = height;
    this.image_url = image;
    this.pokeType = pokeType;
  }
}
